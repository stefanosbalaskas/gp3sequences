% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequence-analysis-audit.R
\name{audit_sequence_analysis}
\alias{audit_sequence_analysis}
\title{Audit a gp3sequences analysis object}
\usage{
audit_sequence_analysis(x, strict = FALSE, tolerance = 1e-08)
}
\arguments{
\item{x}{A gp3sequences analysis result or supported structural object.}

\item{strict}{Logical; if \code{TRUE}, fail when the audit status is \code{"fail"}.}

\item{tolerance}{Numerical tolerance used for matrix/probability checks.}
}
\value{
An object of class \code{gp3_sequence_analysis_audit} containing
\code{summary}, \code{issues}, \code{provenance}, \code{contract}, and \code{status}.
}
\description{
Inspects supported gp3sequences objects for structural contract validity,
recoverable provenance, identifiers, state levels, method settings, and
randomness metadata. The audit does not reinterpret sequence structure as a
psychological, diagnostic, cognitive, emotional, or causal construct.
}
\examples{
sequences <- data.frame(
  sequence_id = rep(c("s1", "s2", "s3"), each = 3L),
  sequence_order = rep(1:3, times = 3L),
  state = c(
    "A", "B", "C",
    "A", "B", "B",
    "C", "B", "A"
  )
)
distance <- compute_sequence_distance(
  sequences,
  method = "levenshtein"
)
audit_sequence_analysis(distance)

}
