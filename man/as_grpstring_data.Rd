% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequence-adapters.R
\name{as_grpstring_data}
\alias{as_grpstring_data}
\title{Create GrpString-compatible event and string inputs}
\usage{
as_grpstring_data(
  data,
  sequence_id_col = "sequence_id",
  order_col = "sequence_order",
  state_col = "state",
  alphabet = NULL
)
}
\arguments{
\item{data}{Long-format sequence data.}

\item{sequence_id_col, order_col, state_col}{Sequence columns.}

\item{alphabet}{Optional single-character symbols. When omitted, printable
ASCII characters are assigned deterministically.}
}
\value{
A list of class \code{gp3_grpstring_input} containing a wide event data
frame, event-name vector, character vector, conversion key, and string vector.
}
\description{
Create GrpString-compatible event and string inputs
}
\examples{
sequences <- data.frame(
  sequence_id = rep(c("s1", "s2", "s3", "s4"), each = 4L),
  sequence_order = rep(1:4, times = 4L),
  state = c("A", "B", "C", "D", "A", "B", "C", "C",
            "D", "C", "B", "A", "D", "C", "A", "A"),
  group = rep(c("g1", "g2"), each = 8L),
  stringsAsFactors = FALSE
)
as_grpstring_data(sequences)

}
